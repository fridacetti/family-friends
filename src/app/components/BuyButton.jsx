const BuyButton = ({ price }) => {
  return (
    <button className="bg-button mx-4 mb-2.5 w-full items-center justify-center gap-4 rounded-full p-3 px-3 text-white">
      <strong>Buy now</strong>
    </button>
  );
};

export default BuyButton;
